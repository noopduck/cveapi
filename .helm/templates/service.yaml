apiVersion: v1
kind: Service
metadata:
  name: {{ include "cveapi.fullname" . }}
  labels:
    {{- include "cveapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cveapi.selectorLabels" . | nindent 4 }}
