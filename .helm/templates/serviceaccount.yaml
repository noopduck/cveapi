{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "cveapi.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "cveapi.labels" . | nindent 4 }}
{{- end }}
